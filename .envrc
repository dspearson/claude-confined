# direnv configuration for claude-confined
# Runs 'just' to show available commands when entering directory

if command -v just >/dev/null 2>&1; then
    just
else
    echo "just is not installed. Install it with:"
    echo "  cargo install just"
    echo "  or visit: https://github.com/casey/just"
fi
